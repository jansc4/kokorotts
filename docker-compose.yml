services:
  kokoro-tts:
    image: ghcr.io/remsky/kokoro-fastapi-gpu:latest
    container_name: kokoro-tts
    restart: unless-stopped
    ports:
      - "8880:8880"
    gpus: all
    # Odkomentuj, jeśli chcesz trzymać cache modeli/voice packów poza kontenerem:
    # volumes:
    #   - kokoro-cache:/root/.cache

# volumes:
#   kokoro-cache: